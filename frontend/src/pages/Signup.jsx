import { useState } from "react";
import { Link, useNavigate } from "react-router-dom";

export default function Signup({ onSignup }) {
  const nav=useNavigate(); const [name,setName]=useState(""); const [email,setEmail]=useState(""); const [password,setPassword]=useState(""); const [confirm,setConfirm]=useState(""); const [error,setError]=useState("");
  const submit=e=>{e.preventDefault();if(password!==confirm){setError("Passwords do not match.");return}if(password.length<6){setError("Password must be at least 6 characters.");return}onSignup(name,email,password);nav("/dashboard")};
  return <div className="auth-page"><div className="auth-left"><Link className="brand auth-brand" to="/"><span className="brand-mark">✓</span>TaskFlow</Link><div className="auth-form-wrap"><span className="eyebrow">GET STARTED</span><h1>Create your workspace.</h1><p className="auth-sub">A simple place to organize your work, study, and everyday tasks.</p><button className="google-btn" onClick={()=>alert("Google OAuth is a frontend placeholder. Connect this button to your FastAPI /auth/google endpoint.")}><span className="google-g">G</span> Continue with Google</button><div className="or"><span>or sign up with email</span></div>{error&&<div className="form-error">{error}</div>}<form onSubmit={submit}><label>Full name<input value={name} onChange={e=>setName(e.target.value)} placeholder="Your name" required/></label><label>Email address<input type="email" value={email} onChange={e=>setEmail(e.target.value)} placeholder="you@example.com" required/></label><label>Password<input type="password" value={password} onChange={e=>setPassword(e.target.value)} placeholder="Create a password" required/></label><label>Confirm password<input type="password" value={confirm} onChange={e=>setConfirm(e.target.value)} placeholder="Repeat your password" required/></label><button className="btn btn-dark full">Create account</button></form><p className="auth-bottom">Already have an account? <Link to="/login">Sign in</Link></p></div></div><div className="auth-art signup-art"><div className="art-glow"/><div className="signup-quote"><span className="quote-mark">“</span><h2>Clarity creates momentum.</h2><p>Break big goals into small, actionable tasks and keep moving forward.</p><div className="mini-person"><span className="avatar">K</span><div><b>Built for focused days</b><small>TaskFlow community</small></div></div></div></div></div>
}